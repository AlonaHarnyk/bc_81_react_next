export interface User {
  name: string;
  email: string;
  isOnline: boolean;
  id: string;
}

export interface Book {
  author: string;
  title: string;
  year: string;
  description: string;
  id: string;
}

export interface Contact {
      
    name: string,
    city: string,
    job: string,
    email: string,
    id: string,
}
